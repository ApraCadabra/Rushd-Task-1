Slug generator Library

This is a Slug generator library built with TypeScript that converts text into a URL-friendly slug (e.g., "Hello World!" → "hello-world").

This project was the first task in Rushd mentoring program.